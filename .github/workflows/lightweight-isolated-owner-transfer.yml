name: Isolated copy owner transfer (disabled)
'on':
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  unsupported:
    runs-on: ubuntu-latest
    steps:
      - name: Explain unsupported service principal owner
        run: |
          echo "::error::Disabled: Power Apps rejected the service principal as canvas app owner on 2026-09-25 (principal type ServicePrincipal; required User). No ownership mutation was attempted by this workflow."
          exit 1
