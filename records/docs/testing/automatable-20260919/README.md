# 実行可能な残作業の検証（2026-09-19）

## 実施結果
全件合格の宣言ではない。準備不足として残していた台帳作成・二形式比較・実行可能なデータ操作試験を実施した。

| 工程 | 今回の結果 | 正式判定に残る条件 |
|---|---|---|
| 1 台帳照合 | 現行6画面816部品/11型・版のプロパティ使用台帳。220表示項目の見出し・参照式と認定簿69項目の参照先。試験データ1645値の型に不一致0 | SRC-02: この使用台帳は公式の版別対応プロパティ一覧ではない。DATA-05: 勤務・保険・税の日本語見出し/順序は既存UI契約との対応を明記したが、独立した業務辞書での裏付けは未確立 |
| 2 配布形式比較 | 6画面を現在のStudioから読み戻し、管理/貼付形式を機械生成して構造一致。全816部品名の重複0。幅変更の対照を不一致として検出 | PATCH-02: 二形式準備不足は解消。現行プロパティ更新方式に対応した独立コンテキストでの全SMK-01が未実施。旧方式の新規YAML貼付成功は主張しない |
| 3 操作試験 | 公開コピーの011で基本9＋5履歴48＋給与詳細163＋認定簿69＝289値、対応見出し/順序を照合。不一致0。0件検索→旧表示なし/出力無効→クリア7件004復帰 | 指定8表示条件・独立コンテキスト・人による受入の代用ではない |
| 4 結果記録 | スクリプト・全6画面の二形式・台帳・期待値と実測を保存 | 完了扱いのために判定を緩めていない |

累積67 PASS / 14 BLOCKED / 5 FAIL / 9 EXCLUDEDは据置。今回の追加証跡だけで未充足の独立台帳や実行環境条件を満たしたことにしない。
前回のNOT_RUN0は分類整理を含むため、全件実施完了を表す数字として使わない。

## 読み方
- `observed-control-catalog.json`: 現在のStudio読戻し816部品。過去814部品の記録を流用しない。11型/版ごとの部品名・使用プロパティ・Variantを列挙。全プロパティの公式対応確認とは別。
- `field-map.json`: 基本9、Work11、Commute5、Social9、Tax8、Payroll15、PayrollModal163の220項目。入力根拠・型・見出し・部品名・参照先を記載。見出しの独立性不足を隠さない。
- 認定簿69項目は既存 `src/staff-master/patches/v1.18/ledger-field-map.json` を参照。`live-ledger-comparison.json` は各表示先の参照先と期待値を検査する。
- `fixture-type-checks.json`: 元給与・通勤定義と7/6レコードの1645値の型照合。空値は許容。業務金額や法定計算の正しさは検証していない。
- `live-011-comparison.json` 57件、`live-payroll-comparison.json` 163件、`live-ledger-comparison.json` 69件。各期待値・実値を保持。名称だけで選択せず職員番号011の値を使用。
- 初回比較ではWork/Taxの終了日2099/03/31と画面空欄が相違。既存の独立期待値証跡（override-20260918/evidence.json、011のEnd空欄）を照合し、予約値の表示変換として明記。実測に合わせて期待値を書き換えたものではない。一般の日付を空欄へ丸めない。
- `six-screen-comparison.json`: 全6画面の現在の読戻しハッシュと二形式一致。画像・変更しない式を再生成せず、Children部分のインデントだけを機械変換した。
- `source-comparison.json`: Screen1単独の対照試験。全6画面の結果は上記別ファイル。
- `Screen1.pa.yaml` 等は検証用の現在定義。新規画面への貼付成功・公開承認済みの配布版を意味しない。App.Formulasは別プロパティであり、このYAMLには含まれない。
- `verify.py`: ネット接続・ブラウザー制御なし。保存済み観測と独立fixtureを比較する。実画面の値取得をこのスクリプトで代替していない。

## 再現
リポジトリのルートで `python docs/testing/automatable-20260919/verify.py` を実行する。PythonとPyYAMLを使用。
ソース・画面値はこのフォルダ、入力fixtureと元定義はリポジトリ正本を参照する。出力JSONとpaste形式を再生成する。ブラウザーの再試験ではない。

## 公式資料の限界
[Microsoftの共通プロパティ説明](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/properties-core)はDisplayMode等の適用対象を記載しているが、ここで使用する全11版の対応プロパティを網羅した版別台帳ではない。取得した説明を、そのまま全使用プロパティ確認済みとは扱わない。

## 保全
今回は製品定義の編集・公開・Dataverse書込みなし。Studioは6画面のコード読戻しのみ。公開コピーは通常データ/標準文字/03会計課ホームへ復帰。検索クリア後7件004を確認。PDF関連/HTML帳票PoCと既知不具合の再試験・修正は実施しない。背景ジョブなし。
