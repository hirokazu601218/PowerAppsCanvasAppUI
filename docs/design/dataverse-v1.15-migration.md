# Dataverse接続移行 v1.15（2026-09-16）

## 実施範囲と状態

- 対象は既存の職員マスタ検索テストアプリ。別Work停止をユーザーが確認した後、Studioの通常のオーバーライド操作で編集を再開。
- 工程6：Dataverse接続、Power Fx置換、5件・25件のStudioプレビュー検証、保存、テスト環境への公開まで完了。
- 工程7：架空25件を投入し全投入値をDataverseから読戻し。run [35080574989](https://github.com/hirokazu601218/PowerAppsCanvasAppUI/actions/runs/35080574989)、job 104743327957、SEED25_VERIFIED。
- 前回の操作基盤停止から復旧。25件の画面確認と公開Player確認を実施。確認範囲は下記の工程7結果を参照。
- 追加権限・ライセンス・従量課金・セキュリティ設定の変更なし。

## ソースの変更

- 職員データ57定義を App.Formulas の StaffBasicView へ集約。
- 旧demoStaff/demοWork等の埋め込みテストテーブル計80定義を除去。ソース内の旧11桁職員番号も0件。
- 職員基本は M_職員基本 から取得。StaffReferenceDate は日本時間の当日。採用日空欄／未来日を採用前、退職日は翌日から退職とする。
- 日付を画面用文字列、勤務時間の分値を H:mm、Choiceを表示文字列に変換。データベースの空欄・0は保持。
- 履歴6種は FirstN(Table(型定義),0) による空一覧。これは実データを生成しない。画面に「履歴未接続」を明示し、住民税履歴欄は非表示。
- 既存画面との型互換のため、旧住民税表示項目は空値のままアダプターに残る。Dataverse列には作成していない。画面改修時に不要な互換列を除去する。
- 今回は100件以下のテスト用構成。100件超では一覧を返さず上限の案内を表示。ForAll／検索結果コレクションの構成を本番の大量データ向け委任設計とみなさない。将来は検索条件をDataverseへ委任してから表示用変換を行う。
- アプリ内の架空25名・固定判定日という旧案内を更新。状態フィルターに採用前を追加。

## 前回までの実施済み検証

|検証|結果|
|---|---|
|DataverseをCountIfで読取|5件。CountRowsのキャッシュ0件を実件数と誤認しないよう修正|
|初期一覧|12桁職員番号の5件を表示|
|採用日空欄／未来日|いずれも採用前|
|在籍／退職|既存5件の期待状態と一致|
|職員番号009900000003で検索|1件、試験 在籍|
|検索条件クリア|5件へ復帰|
|採用前フィルター|2件|
|一致しない文字列で検索|0件。旧職員詳細を表示しない|
|Dataverse再読込|検索条件を戻し5件に復帰|
|未接続履歴|0件、未接続表示。認定簿ボタン無効|
|保存済みパッケージ|2026/9/16 18:34:40保存表示後にダウンロード|
|57か所のYAMLと実行用InvariantScript|期待式と全件一致|
|旧埋め込みデータ定義|80→0|
|25件投入・読戻し|SEED25_VERIFIED、全件の投入値照合|

## 工程7の再開・公開確認（2026-09-16）

- 既存のテスト環境・同じApp IDへv1.15を公開。Studio保存表示は2026/9/16 19:18:42（日本時間）。新しいPlayerタブで初回から「v1.15 ／ Dataverse・25名」を確認。
- 5件の古い一覧が最新データ読込後も残る不具合を検出。btnLoad111.OnSelect／btnClear111.OnSelectで結果キャッシュを再作成せず、検索済みフラグ・選択・ページをリセットして共通ビューへ戻すよう修正。
- 前回保存パッケージとの差分は上記2プロパティ。書出しYAMLとコンパイル済みInvariantScriptの一致、旧埋め込み職員データが残っていないことを確認。
- 検証アカウントは既存の本人セッション。専用テストユーザーのDataverse権限および既存自動P0は今回未検証。自動受入の成功タグはv1.14のままで、v1.15の成功タグは作成しない。
- 権限・課金・MFA・セキュリティ設定の変更なし。

|項目|Studioプレビュー|公開Player|
|---|---|---|
|Dataverse25件、1ページ目20件／2ページ目5件|合格|合格|
|最終ページ021～025、次へ無効|合格|合格|
|025の番号・所属・採用日を詳細表示|合格|未実施|
|採用前10／在籍10／退職5（基準日2026-09-16）|合格|未実施|
|採用日空欄・未来日、採用当日・退職当日の境界|合格|未実施|
|同姓同名011・012の2件検索|合格|合格|
|2件検索後の再読込で25件・1ページ目へ復帰|合格|合格|
|不一致検索0件、旧職員詳細なし|前回5件時に合格|25件で合格|
|条件クリアで25件・1ページ目へ復帰|合格|合格|

この記録はDataverse移行の範囲をブラウザで実操作した結果。全P0、全画面幅、PDF生成、履歴機能の合格を意味しない。

## 残件と次の作業

1. 専用テストユーザーのDataverse実行権限確認、Dataverse fixtureに対応した変更専用テストと既存P0の実行。成功タグの確定は全ゲートの実証後とする。
2. 履歴6種・PDF・帳票は未接続。履歴ER図とテーブル定義は別工程。
3. 本番向け委任検索、サーバー側必須・12桁入力制約は別途実装・検証。
4. 次の自動配布は今回のStudioコンパイル済みbaselineを使用する。旧manifestや旧paste YAMLをそのまま再送して上書きしない。

## 保全

- コンパイル済みmsapp: powerapps/dataverse-v1.15/staff-master.msapp
- SHA-256: 72a0ae0a6abcb866b0b72982ae71e5a106f16401fe2e21d2064a5fe5968c0388
- レビュー用の正本: powerapps/canvas-v3/Src/App.pa.yaml、Screen1.pa.yaml。
- 配布用paste YAMLはこの移行では生成していない。旧paste YAMLによる手動上書きは禁止。データソースとApp.Formulasが必要。
- ダウンロードのAppCheckerに数式コンパイルエラーはなく、既存アクセシビリティ等の指摘は残る。アクセシビリティ全体の合格とは扱わない。
- 自動レビューが「架空の税履歴を残したまま編集する操作」を拒否したため、その案は採用せず、すべての架空履歴を空一覧へ置換して再検証した。
