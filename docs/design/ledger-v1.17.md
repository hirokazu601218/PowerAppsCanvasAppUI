# 認定簿表示・PDF v1.17

対象：自動テスト専用。Issue #35。公開済みv1.16のDataverse職員基本25名、内蔵履歴、職員番号別の認定簿取得を維持する。

- パネル幅を画面幅と帳票幅の小さい方にし、中央配置する。タイトル・職員説明・閉じる・操作欄は同じパネルの中へ収める。
- 100%は幅1122px。表示倍率は50/75/100/125/150/200%の6択。小さい画面や高倍率では帳票を横スクロールする。
- 既存の用紙選択部品を表示倍率選択へ変更する。初期値100%、100%へ戻す操作を備える。内部111 IDは保持する。
- ヘッダーを白背景、タイトルと職員情報を濃青、閉じるを白背景上の青文字・アウトラインとする。
- PDFはA4横、余白0mm、DPI96で固定する。出力中だけ帳票幅1122px、1ページの高さを幅×210/297とし、画面倍率から切り離す。
- 2枚目の既存様式は「新幹線鉄道等利用者」から始まる。出力中はページ間隔0にし、2枚を各A4横のページへ収める。フォーム内の縦横比を維持する。
- PDF保存先フローは既存どおり未接続。今回の対象はPDF生成と表示であり、保存接続追加は行わない。

検証：AUT-LEDGER-117で6倍率の幅、上部全要素の左右境界、配色、職員、PDFの実ページ数とMediaBoxを確認する。実PDFを描画して改ページ位置を視認する。既存7回帰とソース/実行用数式の読戻しも必須。

公開はStudioで保存・公開し、Actionsは読取り検証だけを行う。Dataverseのないv1.14へ戻す旧自動トランザクションは実行しない。

PDFプレビューは画面直下の既存PDFViewerをモーダルより前面へ移動する。Studioの子順と実行時ZIndex（PDF=3、主コンテナー=2）を限定的に照合する。これ以外の構造変更は引き続き拒否する。
