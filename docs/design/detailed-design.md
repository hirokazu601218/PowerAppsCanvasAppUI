# 詳細設計書 v1.07

> 対象アプリ：共通（自動テスト／ハンドメイド）

本書は旧v1.08を基にしたv1.11・v1.14の実装契約と、2026-09-24に確定したSCR-002軽量化設計を収録する。設計・GitHubソース・Studio保存・公開Player・実機受入の状態は区別し、現在地は[STATUS](../handoff/STATUS.md)で確認する。

## 適用区分と参照順

| 区分 | 本書の章 | 扱い |
|---|---|---|
| 現行の承認済み設計 | 10章「SCR-002軽量化設計」 | 旧画面・旧PDF・給与固定モーダルの構成より優先する。2026-09-25の[画面要件9.7・9.8](../requirements/screen-requirements.md#97-画面表示編集の追加要件2026-09-25確定)と競合すれば画面要件を優先する。9.7の編集・保存・破棄確認を実装するための詳細設計は後続で補う。 |
| 旧版の実装契約・回帰根拠 | 1～9章 | v1.11、v1.14の旧内蔵データ、旧Canvas認定簿PDF、旧配置、差分YAML配布等の記録。旧版の試験や復元に用い、現行アプリの実装・合格条件を示さない。 |
| 将来の検討事項 | 本番列との照合、XLSX出力等に触れる箇所 | 過去の検討事項は現在の実装指示・実施済み機能としない。必要なら別途要件と受入条件を確定する。 |

現行の単一アプリはApp ID `204a48dc-7f23-43dd-b934-4654a3cfa306`で、現時点では[隔離`_STUDIO`テーブルと隔離版HTML](../operations/single-app-workflow.md#対象)を使用する。旧章の`Staff`・`Commute`等の件数や`Screen1`の部品表を現行公開版の実測値として扱わない。2026-09-26の[検索試験](../testing/current-app-search-observation.md)には未解消の実測不一致がある。

## 1. ファイルと部品

> **旧版履歴（1～9章共通）：** 以下の部品名・式・テスト件数は当時の仕様。現行版の構成判断は10章と最新の画面要件を参照する。

管理用pa.yamlのScreens/scrStaffMasterSearch_v111/Childrenと貼付用paste.yamlのルート配列を同一にする。v1.11では旧v108と別画面へ導入できるよう識別子を111へ移行した。**v1.12以降は111の識別子を固定し、一括改名しない。** 版はファイル名・表示メタ情報・変更履歴で管理する。

| 機能 | 旧版の部品 | 主なプロパティ |
|---|---|---|
| 検索 | txtKeyword111, ddOrg111, ddStatus111, btnSearch111 | Text/Selected/OnSelect |
| 結果 | galStaff111, lblListTitle111 | Items/Text |
| ページ | btnPrev111, btnNext111, lblPage111 | OnSelect/DisplayMode/Text |
| 詳細 | conPerson111, lblName111 | Visible/Text |
| 履歴 | galWork111, galCommute111, galSocial111, galResident111, galTax111, galPayroll111 | Items |
| 帳票 | btnCertificate111, conLedgerModal111, pdfLedger111 | OnSelect/Visible/Document |
| PDF保存 | tmrLedgerPdf111 | OnTimerEnd（任意フロー接続） |
| サイドバー | conSearchSidebar111, conLeftScroll111, conLeftSurface111, btnSidebarToggle111 | Width/Visible/OnSelect/Tooltip |
| 文字切替 | btnTextSize111, varLargeText111 | OnSelect、各Size/Height/TemplateSize |

モダンコントロールへ変更する際、Text/Value等は実際の対象部品から出力したコードで確認し、旧部品のプロパティ名を流用しない。

## 2. データ契約

職員番号は文字列キー。各履歴にStaffIdを保持し、表示名の一致で結合しない。数値・日付の型と表示文字列を分け、0と空値を区別する。

| データ群 | 旧版のテスト件数 | 保持する内容 |
|---|---:|---|
| Staff | 25 | 基本9項目と検索項目 |
| Work | 27 | 適用期間・単価・勤務条件全項目 |
| Commute | 24 | 通勤認定ID・適用期間・支給方式・月別額等 |
| Social | 25 | 保険・共済・加入等全項目 |
| Resident | 25 | 徴収期間・税額等全項目 |
| Tax | 25 | 税表・固定控除等全項目 |
| Payroll | 25 | 概要列と全項目詳細 |

項目台帳は[field-inventory.json](field-inventory.json)を参照。これはv1.11から機械抽出した部品表示・テストレコードのキー一覧であり、本番データ辞書の代替ではない。基本9項目・履歴列・給与詳細15項目・69帳票フィールドについてv1.08との表示式一致を検査する。旧HTMLの業務書式は[参考詳細設計](../reference/html-v0.807-detailed.md)の8～11章を参照。旧JavaScript構造や接続URLは移植しない。
元HTMLは未知の列も動的表示するため、サンプルにない本番列が存在し得る。本接続前に実データの列台帳を取得して全項目照合すること。推測で列を削らない。

## 3. 状態遷移

| イベント | 更新順と結果 |
|---|---|
| 初期 | Tableを参照、25名、ページ1、先頭職員。起動イベント待ち不要 |
| 検索 | 条件を確定→AND抽出→ページ1→先頭選択または空→旧帳票とPDF解除 |
| クリア | 入力をReset→条件解除→全件→ページ1→先頭選択→旧帳票解除 |
| ページ前後 | ページを1～最終へ制限。結果集合は変えない |
| 行選択 | 選択キー更新→その職員の履歴表示→旧帳票とPDF解除 |
| サイドバー開閉 | 展開フラグだけを反転。検索条件・結果・選択キー・ページは変更しない |
| 認定簿 | 通勤レコード有無確認→対象ID・職員固定→差込→モーダル表示 |
| PDF作成 | 二重押下防止→対象帳票描画完了→生成→成功時Viewer/失敗時エラーと再試行 |
| 閉じる | モーダル閉鎖→元の起動ボタンへフォーカス復帰 |

検索条件は空を無条件、キーワードは前後空白を除去。氏名/番号に加え元仕様の検索対象を維持し、対象列は台帳で明記。番号は数値化しない。全角半角の自動正規化を追加する場合はテストを追加する。
履歴の状態は表示基準日と適用期間から判定し、現行・過去・予定を文字で表示する。未登録と該当なしを根拠なく推定しない。

## 4. 職員検索サイドバー開閉

v1.11で `varSearchSidebarExpanded111` を追加。初期値未設定も展開として扱い、App.OnStartへの依存は増やさない。

| 対象 | 式・仕様 |
|---|---|
| 開閉ボタン OnSelect | モーダル中以外で`Set(varSearchSidebarExpanded111, !Coalesce(varSearchSidebarExpanded111, true))` |
| サイドバー Width | `If(Coalesce(varSearchSidebarExpanded111, true), 360, 48)` |
| 検索内部 Visible | `Coalesce(varSearchSidebarExpanded111, true)` |
| 開閉ボタン | 同一ボタンを常時表示。44×44px。展開時は右上、閉鎖時は操作帯上端 |
| ボタン表示/説明 | 表示文字‹／›。AccessibleLabelとTooltipを「職員検索を閉じる」／「職員検索を開く」に切替 |
| 右詳細X | `conSearchSidebar111.X + conSearchSidebar111.Width + If(Coalesce(varSearchSidebarExpanded111,true),16,0)` |
| 右詳細Width | `Parent.Width - Self.X - 16`。折りたたみで328px拡張する |

- `conSearchSidebar111` は検索内部と常設開閉ボタンの親。`conLeftScroll111.Visible=false`にしてTab移動・読み上げ対象から外す。開閉ごとに別ボタンを生成しない。
- 職員行のOnSelectでは開閉フラグを変更しない。複数職員を続けて確認する操作を妨げない。
- 開閉操作では `txtKeyword`、ドロップダウン、抽出結果、選択職員番号、ページ番号、帳票対象をReset/Clear/Setしない。
- 再展開後は選択行が同じページに見えること。Galleryの任意スクロール量までの厳密な保持は完成条件にせず、選択行が確認できる位置への復帰でよい。
- 開閉後は起点ボタンへフォーカスを保つ。必要な環境では `SetFocus` を使うが、対象コントロールが対応することをStudioで確認する。
- Classic/Buttonには未対応の `AccessibleLabel` を記述しない。実際に出力したコントロールの対応プロパティを確認し、表示TextとTooltipを必ず設ける。
- 1366×768と1920×1080で、展開・折りたたみを繰り返しても右側の表、水平スクロール、モーダル位置が崩れないことを確認する。

## 5. 配置の実装原則

自動レイアウトではFillPortions/LayoutMinWidthを明示し、固定座標と混在させない。手動コンテナ/ギャラリーは各X/Y/Width/Heightを明示。列位置は前列X＋前列Widthから算出し、同じX=0の重複を検出する。
行高は最大セル高さ＋上下余白。ラベルと入力欄の高さ、親高さ、TemplateSizeを合わせる。文字を拡大したら親と行も拡大する。横幅不足は水平スクロールか折返しで解決し、極小幅を許さない。
トークンはデザイン基準に従い集約。ただし今回のデータ初期化不要という条件を維持する。App.Formulasを新依存にするなら単独ファイルと適用手順を必ず添える。

## 6. 帳票・出力

> **旧版履歴：** CanvasのPDFViewer、タイマー・フロー、TSVの当時の契約。現行の通勤認定簿は10.4の別タブHTMLを参照する。

旧Canvas版ではPDFViewerはScreen直下、Documentに生成PDF。モーダルの位置と表示条件は共有する。PDF対象に背面、ボタン、影を含めない。
当時のPDF実験機能ではSaveCommuteLedgerPdfを任意接続フローとし、ファイル入力とfileurl応答を使う。接続しなければ保存完了を表示しない。既存fxはApp.FormulasではなくOnTimerEnd用。
当時は2ページ改ページ、文字の欠け、拡大、保存ファイル名と対象職員、権限を実機で確認する条件だった。A3横起点の試作段階では正式用紙サイズが未決で、帳票背景は空様式PNGを内蔵していた。旧版の用紙条件を現行のHTML認定簿へ転用しない。
TSVコピーは旧版の暫定出力。XLSX実装は将来の検討事項であり、別途設計・承認・受入条件の確定が必要。

## 7. v1.11の部品・配置契約

- 実操作ボタンは`ModernButton@1.0.0`。検索入力は`ModernTextInput@1.0.0`、`Default`で初期値、`Text`で現在値を読む。`TriggerOutput.Keypress`を明示する。
- 組織・状態・用紙の3ドロップダウンは`Classic/DropDown@2.3.1`を維持。`Selected.Value`と`Default`/`Reset`の既存契約を保つための互換性例外。将来のモダン化は実環境コードを確認した個別パッチとする。
- 行選択の透明ヒット領域はClassic Button、表セルはLabel、帳票背景はImageを維持。透明行ボタンはText/Tooltip/可視フォーカスを使い、不正なAccessibleLabelを追加しない。
- `conMain111.Width=Parent.Width`。`conPerson111`を右の縦スクロールの外に配置し、`conRightScroll111.Y=conPerson111.Y+conPerson111.Height+12`とする。
- `conLeftSurface111`は自動レイアウトの子として`FillPortions=0`、最小寸法を自身の寸法に一致させる。孫のX/Yは手動配置。横長表もスクロール親＋寸法明示した手動Surfaceで分離する。
- 基本情報は行高64／大文字72。履歴は48／56。見出しとデータセルは同じ列位置・幅の式を使い、節Yは直前のY+Height+20とする。
- 検索一覧の氏名・在籍状態、番号・所属の4フィールドを保持。不要になった旧4列の見出しコントロールだけを撤去する。
- 勤務履歴に「適用状態」列を追加し、固定基準日2026/09/11による現行・過去・予定を文字表示。サンプルの金額・経路・保険判定を法定計算の検証結果と扱わない。
- 簡易出力は5行／ページ、64px行高。表示ページのみの印刷と全結果TSVコピーを区別し、正式認定簿PDFとは別機能にする。
- モーダル中は背面のボタン・入力・一覧をDisabled。検索／クリア／職員切替は旧帳票・PDFを破棄。開閉自体ではそれらを変更しない。

## 8. v1.12以降

[コントロール単位差分配布方針](../operations/control-diff-policy.md)を適用する。部品IDは111のまま固定。親・子・兄弟の参照を含む最小の完全サブツリーか、対象プロパティのPower Fxを配布する。貼付は追加操作であり自動上書きではない。GitHub完全版と管理版の同期は必須。

## 9. 自動テストアプリv1.14（Issue #23）

`powerapps/canvas-v3/Src/Screen1.pa.yaml`と`automation/change.json`を同期する。部品名111は固定。ここでの`W`は基本情報コンテナーの幅、`i`は0～8、`L`は大きな文字のフラグとする。

| 対象 | 契約 |
|---|---|
| 列数C | `Min(6,Max(1,RoundDown((W-16)/If(L,252,220),0)))` |
| 項目・値の幅、X | `(W-16)/C`、`8+Mod(i,C)*((W-16)/C)` |
| 項目Y、高さ | `52+RoundDown(i/C,0)*If(L,72,64)`、既存28/32 |
| 値Y、高さ | 対応項目のY＋高さ、24/28。上パディング0、下4 |
| conBasic111.Height | `52+RoundUp(9/C,0)*If(L,72,64)`。高さの算出ではSelf.Widthを使う |
| lblName111.Width | 日本語文字をSize×4/3、空白をSize×2/3で見積もり、左右パディングを足す。親幅-120を上限とする。右パディング0、既存Tooltipを維持 |
| lblBadge111 | X=氏名X＋氏名幅＋8。Yは氏名の縦中央に合わせる |
| lblListC2111.Width | 状態lblListC3111のX＋幅－自分のX。2行目の選択色を右端へ揃える |
| lblListC0111 | 元は所属ラベルの背面に重なった番号表示。Text空、Fill=#E2E8F0、Height=1、Width=Parent.TemplateWidth、Y=Parent.TemplateHeight-1で区切り線に転用 |
| conHeader111 | X=`16`、Width=`Parent.Width-2*Self.X`。本文コンテナーの寸法式は維持 |

番号は行ボタンのText/Tooltip、行の読み上げ名、固定サマリーと基本情報に保持する。既に重なっていた番号ラベルだけを線へ転用し、新しい部品やギャラリー構造は増やさない。透明な行ボタンの操作・フォーカスと左選択マーカーを維持する。

アプリは既存のDesignWidth=1366、ScaleToFit=falseを使う。親の利用可能幅から再配置する。表示モードそのものを判別するAPIには依存せず、キャンバス基準幅と広いウィンドウの双方で測定する。[Microsoftのレスポンシブ配置](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-responsive-layout)を参照。

元YAMLで省略された`conHeader111.X=0`は、元msappの実行ルールには存在する。ブリッジはこの部品・プロパティ・既定値の組だけを許可リストに追加する。実行ルールが一意で変更前値0に一致すること、マニフェスト外の差分がないこと、公開後の読戻し一致を引き続き必須とする。他の省略プロパティや構造変更は許可しない。

テストは`AUT-LAYOUT-001`と`AUT-LIST-001`で公開アプリの座標・色・全9項目・操作を測る。旧版v1.13にも旧仕様の期待値で同じ参照先を検証し、復元テストの誤判定を防ぐ。

## 10. SCR-002軽量化設計（2026-09-24）

> **現行の承認済み設計：** 本章は1～9章の旧v1.11／v1.14配置と競合する場合に優先する。2026-09-25の画面要件9.7・9.8は本章より新しいため、編集・表示・未保存変更などの競合は画面要件を優先する。本章への記載だけではGitHubソースと公開アプリの一致や実機受入を示さない。

### 10.1 スクリーンと状態

- 旧`Screen1`の改名先は`scrStaffMasterSearch`。部品名末尾`111`は維持する。現在の公開版での反映状態はSTATUSと読戻しで確認する。
- 改名前に`App.StartScreen`、`Navigate`、`Back`、`App.ActiveScreen`、クロススクリーン参照、E2E locator、差分マニフェスト、ファイル名を検索し、改名後に旧名参照0件を確認する。
- 選択タブは`varStaffDetailTab111`で保持し、許可値を`Basic`、`Work`、`Commute`、`Social`、`Tax`、`Payroll`に限定する。
- 職員変更時は選択履歴、選択通勤GUID、給与表示対象を破棄して新職員の対象へ更新する。検索0件では全て空にする。

### 10.2 ルート配置

| 対象 | 式 |
|---|---|
| 業務画面X | `Parent.Width*4/64` |
| 業務画面Width | `Parent.Width*56/64` |
| 業務画面Y | `Parent.Height*2/30` |
| 業務画面Height | `Parent.Height*26/30` |
| 左検索幅 | 展開時360、折りたたみ時48を開始値とし、業務画面幅を上限に制約 |
| 右詳細 | 左検索の右端＋列間から業務画面右端まで |

比率はPower Apps親描画領域内の配置であり、ブラウザーやOSウィンドウを制御しない。1366×768では左右約85px・上下約51px、1920×1080では左右120px・上下72pxを期待値の起点とし、実描画寸法と丸め値を証跡に残す。

### 10.3 共通タブ・履歴・2列詳細

タブは単一のタブリストまたは反復ギャラリーで生成し、タブごとに完成した詳細パネルを複製しない。基本情報、勤務条件、通勤の選択レコード詳細、社会保険、税固定控除は、項目メタデータを次の表示用レコードへ変換して共通詳細ギャラリーへ渡す。

| 列 | 内容 |
|---|---|
| `Section` | タブ区分 |
| `Order` | 表示順 |
| `Key` | 論理項目キー |
| `Label` | 日本語項目名 |
| `Value` | 型を維持した元値または表示専用値 |
| `DisplayText` | 書式適用後の表示値 |
| `IsBlankValue` | 空欄判定。0と区別 |
| `SourceRecordId` | 選択履歴レコード識別子 |

初版の列数は常に2。長文項目も同じ対象とし、ラベル・値の折返しと行の自動高または計算高で全文へ到達できるようにする。固定高さによる切捨て、省略だけで全文確認不能にすること、列ごとに別ギャラリーを作ることを禁止する。実機試験で不適合となった項目だけ、メタデータの`Span=2`等により全幅化できる設計余地を残す。

履歴区分は小さい履歴一覧と選択詳細を分離する。履歴一覧はレコード選択、期間、状態、識別子に必要な列へ絞り、全業務項目は共通詳細ギャラリーへ表示する。

### 10.4 通勤HTML

- 通勤タブは論理上の`T_通勤`のレコードを選択職員で委任可能に抽出し、認定GUID単位で表示する。現行の単一アプリの接続先は隔離`T_通勤_STUDIO`であり、元テーブルへの切替を指示するものではない。
- 「認定簿表示」は、現在職員、選択レコードの親職員、保持職員、選択GUIDが一致するときだけ有効にする。
- 起動先は認定簿HTML Webリソース版1.01以降。現行アプリでは隔離版HTMLを用い、別タブへ選択GUIDだけを渡し、HTML側でDataverseを再読取りする。元データの正式版へ接続先を切り替えない。
- 軽量化実装で`conLedgerModal111`、`pdfLedger111`、関連PDFタイマー・状態・旧入口・PoC入口を削除する。正式HTMLボタンは残す。削除対象は実装前の部品台帳で確定し、HTML起動・0件・職員切替回帰後に読戻す。

### 10.4.1 固定ヘッダーと本文

- ヘッダーには画面名、画面ID、データ取得日時を置き、操作ボタンを置かない。直下の操作領域からサマリー・基本情報・タブ詳細まで、右側を同じ縦スクロール領域に収める。左検索一覧の内部スクロールと給与表の横スクロールは独立させる。
- 操作領域に「保存」などの存続する操作を置き、「変更を破棄」ボタンは置かない。ホーム・支給対象月・文字サイズ・支給明細遷移・詳細表示等も同領域へ集約する。未保存変更を失う画面・職員遷移や読み取りモードへの切替時は、画面要件9.7に従って破棄を確認する。狭幅では折返し、操作の重なりと画面外への脱落を防ぐ。
- 検索条件を縦一列にする。検索入力の虫眼鏡は入力・キーボード操作を妨げない。検索結果は氏名／番号、状態／部署略称の上下関係とし、在籍・退職・採用前の色と文字を併用する。
- 詳細背景とギャラリー見出しの配色は参照画像に合わせる。「履歴一覧へ戻る」と編集対象フッター、「最新データを読込」「検索結果をTSVで出力」は新しい画面に置かない。
- 通勤タブ右上に「認定簿表示」を設ける。有効条件と別タブHTMLへのGUID連携は10.4に従う。

### 10.5 基準給与簿163項目×動的支給月

- 行メタデータは163項目の`Order`、`Key`、`Label`を一度だけ保持する。
- 表示開始月・表示終了月は年月として保持する。開始月が終了月を超える選択は適用せず、入力エラーを示す。開始・終了は年度や12か月に固定しない。範囲変更時は選択職員の必要な給与レコードを再取得し、列コレクションを再生成する。
- `colPayrollColumns111`は取得済みレコードの`crb3c_payment_date`（支給年月日、和暦文字列）を年月へ正規化し、選択範囲内のレコードだけから生成する。未登録月の空列は作らない。同月に複数ある行はすべて別列にし、月見出しに支給年月日と`crb3c_sequence`（番号）等を併記する。同値の場合も行GUIDで一意に対応付け、支給年月日→番号→行GUIDの順で安定して並べる。月単位の合算、上書き、無条件Firstは行わない。
- 外側を163項目の縦ギャラリー、内側を`colPayrollColumns111`の横ギャラリーとする等、表示レコード数に依存して静的コントロール定義が増えない構造を使う。セルは列の行GUIDと項目Keyで取得済みレコードの値へ対応付ける。
- 初版は採用前・退職後だけを理由に月列を除外しない。登録済みレコード内の0、負数、空欄は区別し、範囲内0件は空状態で示す。支給年月日が任意入力のため、空欄・解釈不能の行は黙って除外せず例外件数と対象を表示する。日付書式および同月複数レコードの登録可否条件は登録処理側で精査し、画面側で一意制約を追加しない。
- SCR-005 `scrPayroll`は月次支給明細・試算画面として維持し、基準給与簿163項目画面と統合しない。

### 10.6 データ取得

- 職員選択確定時に、選択職員キーで必要な履歴を区分単位に一度取得し、表示用コレクションへ変換する。
- ギャラリーの各行・各セルからDataverseへ`LookUp`／`Filter`を繰り返さない。支給月×163項目も取得済みレコードとメタデータから表示する。
- 画面初回表示と他画面からの再入場、ブラウザーのF5による再起動では検索結果を取得し直し、選択職員が引き継がれ有効なら詳細も取得し直す。職員切替時は当該職員の詳細・履歴・通勤・給与を取得する。保存成功後は保存対象を再取得する。表示開始月・終了月の変更時は新しい範囲に必要な給与レコードを取得し直す。タブ切替だけでは再取得せず、画面を開いたままの定期更新は行わない。F5の操作案内を画面に記載しない。
- ヘッダーのデータ取得日時は表示対象の取得成功後に利用者の表示タイムゾーンで更新する。複数データ群の取得中は更新完了と表示せず、失敗時は時刻のみ進めない。職員切替時は旧職員の時刻を現職員の取得時刻と誤認させない。
- 所属部署制限、権限エラー、通信失敗、0件を区別し、失敗時に別職員のキャッシュへ戻さない。

### 10.7 削除、部品数、完了条件

削除候補は旧Canvas認定簿・PDF・PoC、給与163項目固定モーダル、非表示住民税、タブごとの重複詳細、未参照コントロール。Git履歴を削除せず、正式画面ソースから除去する。

改修前の当時の基準は全6画面816、旧`Screen1`657コントロール。改修後は画面別・型別に再集計し、`scrStaffMasterSearch`の150～210を当時の設計目標として比較する。さらにApp checker、Studio読込み・選択・保存、公開Player起動、検索、職員切替、タブ切替、HTML起動、給与初期表示・横移動を測定する。文書、ローカルYAML、Studio保存、公開、実機受入をそれぞれ区別して記録する。

## 変更履歴

| 版 | 内容 |
|---|---|
| 1.07 | 1～9章の旧版履歴と10章の承認済み設計を区分。9月25日の破棄確認、現行の隔離接続先、実装・試験状態の区別を明示 |
| 1.06 | SCR-001スマホ操作、SCR-002の固定ヘッダー・スクロール・検索一覧・ボタン移動削除・取得契機と時刻を追加 |
| 1.05 | SCR-002軽量化の画面名、共通ギャラリー、通勤HTML、動的月列、データ取得、削除・測定契約を追加 |
| 1.04 | 自動テストv1.14の配置式、番号ラベル転用、既定Xの限定同期、実測E2E契約を追加 |
| 1.03 | v1.11実装名、モダン部品、固定サマリー、状態保持、文字切替、差分契約へ更新 |
| 1.02 | v1.09向け職員検索サイドバーの変数、部品、式、状態保持、アクセシビリティを追加 |
| 1.01 | 現行部品・データ・B案状態遷移・配置・PDFの具体的実装契約を定義 |
