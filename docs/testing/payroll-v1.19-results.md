# 基準給与簿アプリ v1.19 公開確認

## 実装と対象

既存の隔離Developer環境・同じアプリにT_基準給与簿を追加。親の職員番号でFilterし、支給年月日・整理番号の降順に表示する。現在の和暦文字列は令和08年と2桁月日で統一されているため順序を検証できた。他元号や不統一書式の追加時は日付型の並び替えキーを別途設計する。

内蔵Payroll 7件をApp.Formulasとfixtureから削除。給与一覧、詳細、主要15項目のTSV出力を共通のStaffPayrollHistoryに統一。163項目すべての新規画面は今回追加していない。未確定のWork/Social/Taxは継続。

## 公開前Studio検証

数式エラー0。職員003: 6月/5月/4月の3件、返納-2,000・追給5,000、総額253,000/260,000/250,000、現金243,000/250,000/240,000を確認。004、011、012、005の各1件を確認。同姓同名011/012の現金230,000/250,000は分離。005の0を空欄にせず表示。001/025の給与0件と出力不可、旧職員データが残らないことを確認。003の詳細とTSVは最新6月に一致。画面レイアウトもスクリーンショットで確認。

## 公開後の正確な照合

run 35182918673で公開版を取得。SHA-256 `dd0372d0a6e052e055682959b855e5e9c38e8f9d0b5d316852bb334c1f2bd420`。

45件の意図した変更と9件の既存通勤列のStudio表示名正規化を確認。9件はメタデータの対応どおりで処理は同一。全361コントロール（Appを含む）の名前集合を維持。公開アーカイブのYAML・コンパイル済みルール・接続定義と保存版を照合済み。再生成用fixtureはWork7/Social5/Tax5で一致。

**未完了: 再読込ボタンの給与Refresh追加1式。** 公開パッケージでは旧式のまま。初回読込、職員切替、表示、出力の動作は確認したが、給与テーブル更新直後の明示再読込は未完了として扱う。

補正操作はブラウザー自動承認レビューが拒否。理由は「originへの接続再確立で未保存の数式変更を失う可能性」。読み取り専用のDOM確認も同じ理由で拒否された。別経路の書込みで迂回していない。pending_changesに予定式を保持。

専用テストユーザー `powerapps-test@govaca.onmicrosoft.com` による[自動受入 run 35183359299](https://github.com/hirokazu601218/PowerAppsCanvasAppUI/actions/runs/35183359299): **10件合格、0件失敗（4.2分）**。給与7件・0件・同姓同名・金額0・正負の調整・最新支給日のTSVに加え、既存の職員検索/P0、4件のUI回帰、通勤6件の認定簿とPDFを検証。最後の公開版再取得も上記SHAと一致し、ソース54プロパティ・コンパイル済みルール・接続定義は完全一致。

給与テーブル更新後のキャッシュ鮮度検証は、明示Refreshが未反映のため未実施。自動テストの「refresh」は変更なしのデータを再表示する回帰確認であり、更新後の鮮度保証ではない。

Studioのその他の検出: アクセシビリティ533件、性能11件。今回の参照元変更では改修対象外。基準版との差分数は未測定。

## 工程ごとの結果

| 工程 | 状態 | 証跡・残件 |
|---|---|---|
| ①現行版・データ・権限確認 | 成功 | v1.18とテーブル7件、既存Readerを確認 |
| ②接続・Fx整理 | 一部未完了 | 給与移行45プロパティ反映、再読込1式は自動承認レビューで拒否 |
| ③表示・回帰テスト | 成功 | Studio確認、自動受入10件、ローカル50件 |
| ④公開・読戻し | 成功 | v1.19公開、2回の公開版取得SHA一致 |
| ⑤GitHub記録 | PR #50に記録 | 公開版を共有正本へ反映し、Issue #49は残件として維持 |

承認済みの未反映式は `src/staff-master/patches/v1.19/manifest.json` の `pending_changes` に保存。再開時は既存IfError内に給与Refreshを追加し、保存・公開・読戻しと給与更新後の再読込確認を行う。ブラウザーの接続再確立で未保存編集を失う可能性に対するユーザー確認が必要。別経路のアプリ書込みで今回の拒否を迂回しない。


今回の追加権限、課金設定、ライセンス購入はなし。読取り権限は給与テーブル構築時に付与済みの専用Readerを利用。

ローカル自動検査50件は合格。旧版変更manifestに依存していた2件のポリシーテストは固定の合成fixtureへ独立させ、対象違い・未承認の順序変更・before/after不一致・重複変更を拒否する検査を維持。通勤fixture保持検査は給与移行を明示的に反映。ポリシー実装自体は変更なし。
