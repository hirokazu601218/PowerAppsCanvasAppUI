# v1.22 回帰試験計画（Issue #54）

開始2026-09-18 04:23:59 UTC、停止期限05:23:59 UTC。対象はmain dc6970b、公開済みUI試作v1.22。追加画面はダミー計算・セッション内仮登録という承認済み範囲で評価する。

## 現行仕様に合わせた期待値

- 独立fixtureは tests/fixtures/staff-basic-25.json、commute-6.json、payrollledger-7.json、163列定義は config/dataverse/payrollledger-columns.json。
- 旧D0の山田等の25名は使用しない。現行の12桁番号と架空職員を使う。
- 03会計課7名、02総務課8名、01秘書課8名。所属未登録001/005の2名は確認用所属3択から到達しない。全25名を表示することを正解にしない。
- 同姓同名011/012は所属が異なるため、所属を切り替えて別々に選択し履歴が混在しないことを検証する。
- 1所属の件数は20未満。前へ／次への無効は検証し、20/21/40件のページ境界は追加fixtureがなくBLOCKEDとする。実テーブルを変更して準備しない。
- 給与詳細はv1.21で163項目に拡大済み。旧15項目の期待値は163列定義と独立7件fixtureで置き換える。所属未登録005の給与0円は現行UIから到達できないため、そのレコードの実機照合はBLOCKED。追加支給明細の登録済み0円は別ケースで検証。

## 実行セット

| セット | 範囲 |
|---|---|
| 静的・基盤 | 既存automation単体50件、YAML構造・ID・貼付形式、所属fixtureと列マップ |
| SMK | 新規アプリセッション3回：起動、検索、職員選択、検索欄開閉、0件、クリア、ページ端、認定簿2枚 |
| CORE | 空白・複合・所属・状態・項目検索、未確定入力、重複クリック、基本／履歴、同姓同名、163給与項目、TSV・出力ページ、再読込、20周反復、キーボード |
| SCR-001～006 | 遷移、未選択、月変更、0円／未登録、部分欠勤1分と最終端数、支給率仮登録、管理者表示、初期化、内訳開閉 |
| REPORT | 通勤6件・認定ID・日付・金額・空経路、認定簿6倍率、実PDF2ページとA4横寸法、プレビュー復帰 |
| VIS | 1366×768／1920×1080 × 標準／大文字 × サイドバー開／閉の8条件。追加5画面は900×600、1100×800、1366×768、1920×1080で20表示 |

認証は既存の専用テストユーザーと公式pinned toolkitを使用。GitHub Actionsで独立した試験用ブラウザーを起動する既存方式。Cloud BrowserではStudioの確認と必要な修正を行う。いずれもDataverse書込み、権限変更、旧パッケージの再インポートはしない。

## 独立して残す制約

PDFの保存先フローと保存失敗注入は未接続のためBLOCKED。実ユーザーの読み上げ評価、OS印刷ダイアログ、長文／40名の追加fixture、破壊的な試験器変異は未準備であり合格としない。試験実行時にはケースごとの結果・失敗原因・実機証跡を追記し、全合格と未実施を混同しない。
