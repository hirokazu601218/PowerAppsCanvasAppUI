# 今回の格納前検査

## v1.11（2026-09-13）

対象：v1.08正本から作成したB案＋職員検索サイドバー開閉の試作候補。廃止したv1.09/v1.10は使用していない。

| 検査層 | 結果 |
|---|---|
| YAML・2形式同期 | 重複キー、構造、管理版Childrenと貼付版配列の一致を確認 |
| 部品・参照 | 355部品をローカル許可リストで確認。PDFViewerは画面直下。旧108参照なし |
| 数式構文 | 7,393プロパティ式を限定Power Fxパーサーで解析 |
| 回帰・状態・寸法 | 68チェック合格。初期25名、検索、選択、ページ、帳票差込、0件、旧PDF破棄、開閉保持、大文字、8組の配置式、既知エラー再導入検出 |
| 項目保持 | 基本9項目、6履歴群・給与詳細の表示式、69帳票フィールドを旧版と照合。帳票背景は同一 |
| 描画モデル | 1366×768／1920×1080 × 展開／閉鎖 × 標準／大文字の8枚＋下部スクロール2枚を作成。モデル内の文字高さ不足0件 |
| 目視 | 代表状態の配置モデルで検索欄、選択マーカー、固定職員サマリー、全9項目、履歴の列・行の分離、下部セクションを確認 |
| Power Apps Studio貼付・実操作 | **未実施** |
| Microsoft公式型検査・ネイティブ描画 | **未実施** |
| PDF生成・保存フロー・改ページ | **未実施** |
| 支援技術・200%拡大・実フォント校正 | **未実施** |

詳細：[v1.11機械可読結果](v1.11-validation.json)。YAMLのSHA-256で検査対象を識別する。

実行：`python tests/validate_v111.py`。描画：`tests/render_v111_model.py`＋Pillow＋Noto Sans JP Regular/Semibold。描画は実YAMLの寸法式を使うが、Segoe UI／日本語フォールバック、Fluent部品、スクロールバー、支援技術を再現したものではない。生成AI画像を実機証跡として用いていない。一時PNGとフォントはGitHubへ格納しない。

### 今回見つけて修正した点

- 大きな文字で検索見出し・一覧ラベル・氏名欄の高さ／パディングが不足する点を修正。
- 古い固定Yの履歴凡例が、可変高さのセクションへ重なる点を修正。
- 0件でも出力ボタンを押せる既存挙動を無効化し、基本情報の空カードも非表示にした。
- 検索・クリア・職員変更で旧帳票と生成PDFを明示的に破棄するよう修正。
- 簡易出力を5行／ページへ調整し、768px高でも表示ページの行が収まるようにした。

### 保留事項

R07のPDF実行・保存、認定ID選択と通勤金額の整合、本番全項目、正式XLSX、キーボード・読み上げ・200%拡大は未完了。v1.12の実パッチはまだなく、T14は今後の変更時に実施する。68チェック合格を全要件／実機合格と読み替えない。

## v1.08の基準版検査記録

対象：職員検索v1.08。B案の画面改修は未実施。

実行：`python tests/validate_v108.py`

- YAML重複キー・構造・2形式一致：合格。
- 352コントロールのローカル許可リスト検査：合格。
- 7,127式の限定Power Fx構文解析：合格。
- 検索・初期表示・ページング・帳票差込等47ケース：合格。
- 詳細：[機械可読結果](v1.08-validation.json)。ハッシュで対象YAMLを識別。

Studioでの貼付・描画・操作、公式コンパイラ型検査、PDF生成・保存は未実施。47ケースはローカル評価器の範囲のみ。モーダル表示中の職員切替や全項目保持・金額整合等、次版の受入条件をすべて満たす検証ではない。

旧v1.08導入書にある配置5,632項目比較は当時の検査記録であり、今回再実行した検査には含まない。
